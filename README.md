# 🤖 AI-NK - Автоматизированная проверка нормативной документации

## 🚀 Быстрое развертывание

### Готовый Docker образ (Рекомендуется)

**Скачайте готовый образ и запустите одним командой:**

```bash
# Скачать образ
wget https://github.com/your-repo/ai-nk/releases/download/v1.0.0/ai-nk-v1.0.0.tar.gz

# Загрузить образ
docker load < ai-nk-v1.0.0.tar.gz

# Запустить систему
docker run -d \
  --name ai-nk \
  -p 80:80 \
  -p 8001:8001 \
  -p 8002:8002 \
  -p 8003:8003 \
  -p 8004:8004 \
  -v ai-nk-data:/app/data \
  -v ai-nk-logs:/app/logs \
  -v ai-nk-uploads:/app/uploads \
  ai-nk:latest

# Открыть в браузере
open http://localhost
```

### Автоматическое развертывание из исходного кода

```bash
# Клонирование и развертывание
git clone <repository-url>
cd AI-NK
chmod +x deploy.sh
./deploy.sh --deploy
```

## 📋 Описание системы

AI-NK - это интеллектуальная система для автоматизированной проверки нормативной документации с использованием искусственного интеллекта. Система обеспечивает:

- 📄 **Загрузку и парсинг документов** (PDF, DOCX, DWG, IFC)
- 🔍 **Автоматическую индексацию** с использованием BGE-M3
- 🤖 **ИИ-проверку нормоконтроля** с помощью LLM
- 📊 **Детальную отчетность** в PDF формате
- 🔄 **Асинхронную обработку** больших документов
- 📈 **Мониторинг и логирование**

## 🏗️ Архитектура

```
┌─────────────────┐    ┌─────────────────┐    ┌─────────────────┐
│   Frontend      │    │   API Gateway   │    │  Document       │
│   (React)       │◄──►│   (FastAPI)     │◄──►│  Parser         │
└─────────────────┘    └─────────────────┘    └─────────────────┘
                                │                       │
                                ▼                       ▼
                       ┌─────────────────┐    ┌─────────────────┐
                       │   RAG Service   │    │   Rule Engine   │
                       │   (Vector DB)   │    │   (LLM)         │
                       └─────────────────┘    └─────────────────┘
                                │
                                ▼
                       ┌─────────────────┐
                       │   PostgreSQL    │
                       │   (pgvector)    │
                       └─────────────────┘
```

## 🔧 Компоненты

| Сервис | Порт | Описание |
|--------|------|----------|
| **Frontend** | 80 | React веб-интерфейс |
| **API Gateway** | 8004 | Маршрутизация запросов |
| **Document Parser** | 8001 | Парсинг документов |
| **RAG Service** | 8002 | Векторный поиск |
| **Rule Engine** | 8003 | Проверка норм |
| **PostgreSQL** | 5432 | База данных |
| **Redis** | 6379 | Кэширование |
| **Qdrant** | 6333 | Векторная БД |

## 🛠️ Управление системой

### Основные команды

```bash
# Полное развертывание
./deploy.sh --deploy

# Проверка здоровья
./deploy.sh --health

# Остановка системы
./deploy.sh --stop

# Обновление системы
./deploy.sh --update

# Интерактивное меню
./deploy.sh --menu
```

### Docker команды

```bash
# Просмотр логов
docker logs ai-nk-system

# Подключение к контейнеру
docker exec -it ai-nk-system bash

# Перезапуск системы
docker-compose -f docker-compose.prod.yaml restart
```

## 📊 Мониторинг

### Проверка состояния

```bash
# Основной health check
curl http://localhost/health

# Проверка API Gateway
curl http://localhost:8004/health

# Проверка Document Parser
curl http://localhost:8001/health
```

### Логи системы

```bash
# Основные логи
docker logs ai-nk-system

# Логи приложения
docker exec ai-nk-system tail -f /app/logs/*.log
```

## 🎯 Функциональность

### Загрузка нормативных документов
- Поддержка форматов: PDF, DOCX, DWG, IFC, TXT
- Автоматическое определение типа документа
- Проверка на дубликаты
- Асинхронная обработка с отображением прогресса

### ИИ-проверка нормоконтроля
- Постраничная проверка документов
- Использование LLM для анализа
- Детальная отчетность с указанием отклонений
- Экспорт отчетов в PDF

### Векторный поиск
- Индексация с использованием BGE-M3
- Семантический поиск по документам
- Поддержка pgvector для PostgreSQL
- Интеграция с Qdrant

## 🔧 Конфигурация

### Переменные окружения

```yaml
# docker-compose.prod.yaml
environment:
  - MAX_NORMATIVE_DOCUMENT_SIZE=104857600  # 100MB
  - MAX_CHECKABLE_DOCUMENT_SIZE=104857600  # 100MB
  - POSTGRES_PASSWORD=norms_password
```

### Настройка портов

По умолчанию система использует:
- **80** - Frontend (HTTP)
- **443** - Frontend (HTTPS)
- **8001** - Document Parser
- **8002** - RAG Service
- **8003** - Rule Engine
- **8004** - API Gateway

## 🚨 Устранение неполадок

### Частые проблемы

#### Порт уже занят
```bash
# Проверка занятых портов
lsof -i :80
lsof -i :8001

# Изменение портов в docker-compose.prod.yaml
```

#### Недостаточно памяти
```bash
# Проверка доступной памяти
free -h

# Увеличение swap
sudo fallocate -l 2G /swapfile
sudo chmod 600 /swapfile
sudo mkswap /swapfile
sudo swapon /swapfile
```

#### Проблемы с базой данных
```bash
# Сброс базы данных
docker exec ai-nk-system rm -f /app/data/.initialized
docker-compose -f docker-compose.prod.yaml down
./deploy.sh --deploy
```

## 📈 Масштабирование

### Горизонтальное масштабирование
```yaml
# docker-compose.prod.yaml
services:
  document-parser:
    deploy:
      replicas: 3
  rag-service:
    deploy:
      replicas: 2
```

### Вертикальное масштабирование
```yaml
# docker-compose.prod.yaml
services:
  ai-nk:
    deploy:
      resources:
        limits:
          memory: 8G
          cpus: '4.0'
```

## 🔒 Безопасность

### Рекомендации
1. Измените пароли по умолчанию
2. Используйте HTTPS в продакшене
3. Настройте файрвол
4. Регулярно обновляйте систему
5. Мониторьте логи

## 📞 Поддержка

### Полезные команды

```bash
# Полная диагностика
./deploy.sh --health
docker system df
docker stats

# Экспорт логов
docker logs ai-nk-system > ai-nk-logs.txt

# Создание бэкапа
docker exec ai-nk-system pg_dump -h localhost -U norms_user norms_db > backup.sql
```

### Документация

- [📖 Подробное руководство по развертыванию](DEPLOYMENT.md)
- [🚀 Быстрое развертывание](QUICK_DEPLOY.md)
- [🎓 Руководство по обучению системы](TRAINING_GUIDE.md)
- [⚡ Быстрый старт обучения](QUICK_START_TRAINING.md)

## 📄 Лицензия

MIT License - см. файл [LICENSE](LICENSE) для деталей.

---

**AI-NK System** - Автоматизированная проверка нормативной документации 🤖

*Создано с ❤️ для автоматизации процессов нормоконтроля*
